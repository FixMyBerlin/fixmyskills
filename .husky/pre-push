#!/usr/bin/env sh
set -e

bun run fmt

if ! git diff --quiet --exit-code; then
  printf '\n%s\n' \
    'Oxfmt reformatted markdown files.' \
    'Review the diff, commit the changes, and push again.' \
    >&2
  exit 1
fi
